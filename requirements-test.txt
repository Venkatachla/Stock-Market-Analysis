# =========================
# Test Dependencies
# =========================
# These packages are required for running the backend test suite.
# Install via: pip install -r requirements-test.txt
#
# NOTE: Production deps (requirements.txt) must be installed first.
# =========================

pytest>=7.0
pytest-cov>=4.0
httpx>=0.24,<1.0

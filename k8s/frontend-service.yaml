apiVersion: v1
kind: Service
metadata:
  name: stcok-frontend
  namespace: stcok
  labels:
    app: stcok-frontend
spec:
  type: NodePort
  ports:
  - port: 5173
    targetPort: 5173
    nodePort: 30001
    protocol: TCP
    name: http
  selector:
    app: stcok-frontend

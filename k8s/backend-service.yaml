apiVersion: v1
kind: Service
metadata:
  name: stcok-backend
  namespace: stcok
  labels:
    app: stcok-backend
spec:
  type: NodePort
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30000
    protocol: TCP
    name: http
  selector:
    app: stcok-backend

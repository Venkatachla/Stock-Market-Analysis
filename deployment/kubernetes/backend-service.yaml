apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: stockpulse
    component: backend
spec:
  type: ClusterIP
  selector:
    app: stockpulse
    component: backend
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000

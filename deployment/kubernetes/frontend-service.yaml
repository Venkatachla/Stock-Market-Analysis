apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: stockpulse
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: stockpulse
    component: frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80

apiVersion: v1
kind: ConfigMap
metadata:
  name: stockpulse-config
  namespace: default
data:
  ENVIRONMENT: production
  LOG_LEVEL: info
  DATABASE_HOST: postgresql
  DATABASE_PORT: "5432"
  DATABASE_NAME: stockpulse
  REDIS_HOST: redis
  REDIS_PORT: "6379"
  API_TIMEOUT: "30"
  MAX_CONNECTIONS: "100"
